{
  "name": "ad-muter",
  "version": "0.5.0",
  "private": true,
  "description": "Browser extension that mutes the tab (and optionally blurs the player) during ad breaks in supported web players. No blocking, no tracking, no network access.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "npm run build && node scripts/zip.mjs",
    "icons": "node scripts/gen-icons.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ci": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
